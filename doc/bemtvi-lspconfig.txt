*bemtvi-lspconfig.txt*            Ready-made btv.lsp server configs for bemtvi

==============================================================================
Table of Contents                         *bemtvi-lspconfig-table-of-contents*

1. Install                                          |bemtvi-lspconfig-install|
2. Setup                                              |bemtvi-lspconfig-setup|
3. Overriding a server                  |bemtvi-lspconfig-overriding-a-server|
4. The native path                          |bemtvi-lspconfig-the-native-path|
5. Keymaps                                          |bemtvi-lspconfig-keymaps|
6. Bundled servers                          |bemtvi-lspconfig-bundled-servers|
7. Trying it locally                      |bemtvi-lspconfig-trying-it-locally|

Ready-made `btv.lsp` configurations for the most widely-used language servers
— a native port of nvim-lspconfig for bemtvi.

Each server ships as a curated config table (command, filetypes, root markers,
and sensible default settings) that bemtvi's own `btv.lsp` control surface
understands directly. There is NO neovim compatibility layer — the presets
are plain data driven onto the documented `btv.lsp.*` API, so nothing blocks
and nothing is intercepted.

This plugin only configures servers; it does not install them. Install the
language server binaries yourself (see Bundled servers for each install
command). A missing binary fails LOUD — bemtvi never pretends a server that
isn't there is working.

                                                              *bemtvi-lspconfig*


==============================================================================
1. Install                                          *bemtvi-lspconfig-install*

Declare it with the built-in `:Plugins` manager, then call `setup()` with the
servers you want:

>lua
    btv.plugins({
      {
        "davidrios/bemtvi-lspconfig",
        config = function()
          require("bemtvi-lspconfig").setup({
            servers = { "lua_ls", "pyright", "gopls", "rust_analyzer" },
          })
        end,
      },
    })
<

Run `:PluginSync` to clone it. Open a file of a matching type and, if its
server is installed, it attaches automatically.


==============================================================================
2. Setup                                              *bemtvi-lspconfig-setup*

`require("bemtvi-lspconfig").setup(opts)` is the one-call entry point:

>lua
    require("bemtvi-lspconfig").setup({
      -- Which servers to enable. One of:
      --   "all"                          every bundled server
      --   { "lua_ls", "pyright" }        a list of names (bundled defaults)
      --   { lua_ls = { settings = … } }  a map of name -> overrides
      --   { "gopls", eslint = false }    mixed; `false` skips a server
      servers = { "lua_ls", "pyright" },
    
      -- Applied to every server (the "*" base layer):
      capabilities = {},        -- client capabilities to broadcast
      settings = {},            -- settings merged into every server
      root_markers = {},        -- extra project-root markers
      on_attach = function(client, bufnr) end,
    
      -- Convenience toggles:
      keymaps = true,           -- install the default LSP keymaps (see Keymaps)
      inlay_hints = false,      -- enable inlay hints for servers that provide them
    })
<

`setup()` is additive — call `require("bemtvi-lspconfig").enable({ … })`
later to turn on more servers with the same accepted shapes (`"all"`, a list, a
name→overrides map, or a mix with `false` to skip).


==============================================================================
3. Overriding a server                  *bemtvi-lspconfig-overriding-a-server*

Pass a table as the server's value to deep-merge your changes over the
bundled preset:

>lua
    require("bemtvi-lspconfig").setup({
      servers = {
        "lua_ls",
        gopls = {
          settings = { gopls = { gofumpt = true } },
        },
        rust_analyzer = {
          settings = { ["rust-analyzer"] = { check = { command = "clippy" } } },
        },
      },
    })
<


==============================================================================
4. The native path                          *bemtvi-lspconfig-the-native-path*

Because the presets live on the plugin's runtimepath as `lsp/<name>.lua`,
bemtvi's `btv.lsp` finds them on its own. Once the plugin is installed you
can skip `setup()` entirely and use the engine API directly:

>lua
    btv.lsp.config("rust_analyzer", {                     -- optional overrides
      settings = { ["rust-analyzer"] = { check = { command = "clippy" } } },
    })
    btv.lsp.enable("rust_analyzer")
<


==============================================================================
5. Keymaps                                          *bemtvi-lspconfig-keymaps*

bemtvi already installs the core LSP maps buffer-local when a server attaches:
`gd`, `gD`, `gr`, `K`, and `<C-k>` (insert). With `keymaps = true` (the
default) this plugin adds the rest of the now-standard set on top, at the
overridable rung so your own maps always win:

>
    grn          rename
    gra          code action
    grr          references
    gri          implementation
    grt          type definition
    gO           document symbols
    <leader>ls   workspace symbols
    <leader>lf   format buffer
    <leader>lh   toggle inlay hints
<

Set `keymaps = false` to manage all of these yourself.


==============================================================================
6. Bundled servers                          *bemtvi-lspconfig-bundled-servers*

Each name is a `lsp/<name>.lua` preset. This plugin configures them; install
the binary yourself.

>
    bashls         Bash, sh                npm i -g bash-language-server
    clangd         C, C++, ObjC, CUDA      ships with LLVM/Clang
    cssls          CSS, SCSS, Less         npm i -g vscode-langservers-extracted
    dockerls       Dockerfile              npm i -g dockerfile-language-server-nodejs
    eslint         JS/TS linting           npm i -g vscode-langservers-extracted
    gopls          Go                      go install golang.org/x/tools/gopls@latest
    html           HTML                    npm i -g vscode-langservers-extracted
    intelephense   PHP                     npm i -g intelephense
    jsonls         JSON, JSONC             npm i -g vscode-langservers-extracted
    lemminx        XML, XSD, XSL, SVG      LemMinX binary / jar
    lua_ls         Lua                     https://luals.github.io/#install
    marksman       Markdown                github.com/artempyanykh/marksman/releases
    omnisharp      C#, VB                  github.com/OmniSharp/omnisharp-roslyn/releases
    pyright        Python                  npm i -g pyright
    ruby_lsp       Ruby, ERB               gem install ruby-lsp
    rust_analyzer  Rust                    rustup component add rust-analyzer
    tailwindcss    Tailwind CSS            npm i -g @tailwindcss/language-server
    taplo          TOML                    cargo install --features lsp taplo-cli
    terraformls    Terraform               github.com/hashicorp/terraform-ls/releases
    ts_ls          JavaScript/TypeScript   npm i -g typescript typescript-language-server
    vimls          Vimscript               npm i -g vim-language-server
    yamlls         YAML                    npm i -g yaml-language-server
<

Don't see one you need? You can still point `btv.lsp.config` /
`btv.lsp.enable` at any server of your own — this plugin just bundles the
common ones with good defaults.


==============================================================================
7. Trying it locally                      *bemtvi-lspconfig-trying-it-locally*

This repo ships a runnable demo (enables `lua_ls` for a sample file):

>sh
    BEMTVI_CONFIG=examples bemtvi examples/sample.lua
<

(run from a checkout of this repo). With `lua-language-server` installed: hover
with `K`, jump with `gd`, rename with `grn`.

Generated by panvimdoc <https://github.com/kdheepak/panvimdoc>

vim:tw=78:ts=8:noet:ft=help:norl:
